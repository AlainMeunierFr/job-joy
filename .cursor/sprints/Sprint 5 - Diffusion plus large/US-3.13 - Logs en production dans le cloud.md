# US-3.13 — Logs en production dans le cloud (emplacement, niveau, rotation)

Emplacement des logs en production (fichier dans userData ?, niveau log, rotation). Optionnel : envoi des logs vers un service cloud pour diagnostic à distance (avec accord utilisateur).
