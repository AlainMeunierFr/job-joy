# =============================================================================
# Job-Joy — Variables d'environnement (dossier de données)
# =============================================================================
# Ce fichier est copié ici au premier lancement. La clé de chiffrement est
# complétée automatiquement. Vous pouvez éditer ce fichier pour ajouter les
# variables optionnelles ci-dessous, puis redémarrer l'application.
# =============================================================================

# Clé de chiffrement des clés API (Airtable, etc.) et du mot de passe IMAP
# dans parametres.json. Complétée au premier lancement ; ne pas supprimer.
PARAMETRES_ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# Optionnel : authentification Microsoft (connexion « Microsoft »)
# -----------------------------------------------------------------------------
# Sans ces variables, l'app réutilise la dernière connexion Microsoft enregistrée
# (parametres.json). Si vous voyez une erreur mentionnant un tenant d'entreprise
# (ex. "HOLDING JARLE") alors que vous utilisez Outlook/Hotmail perso, définissez
# AZURE_TENANT_ID=common et une app Azure autorisant les « comptes personnels ».

# ID client de l'application enregistrée dans Microsoft Entra (portail Azure).
# AZURE_CLIENT_ID=

# ID de l'annuaire : pour compte pro = ID de l'annuaire Entra ; pour Outlook perso = common
# AZURE_TENANT_ID=common

# Secret client de l'application Azure (flux confidentiel, optionnel selon le type d'app).
# AZURE_CLIENT_SECRET=
