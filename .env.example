# =============================================================================
# Job-Joy — Variables d'environnement (exemple)
# Copier vers .env.local et renseigner les valeurs selon le contexte.
# En mode packagé (Electron), le .env.local est dans %APPDATA%\job-joy\
# et PARAMETRES_ENCRYPTION_KEY peut y être générée automatiquement.
# =============================================================================

# -----------------------------------------------------------------------------
# Obligatoire en développement (et pour l’app en release)
# -----------------------------------------------------------------------------

# Clé de chiffrement (32 octets) pour les clés API et le mot de passe IMAP
# dans parametres.json. Génération : openssl rand -hex 32
# En packagé, créée automatiquement dans userData/.env.local si absent.
PARAMETRES_ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# Auth Microsoft (optionnel — peut aussi être renseigné dans l’app)
# -----------------------------------------------------------------------------

# ID client de l’application enregistrée dans Microsoft Entra
AZURE_CLIENT_ID=

# ID de l’annuaire Entra (Vue d’ensemble de l’app > ID de l’annuaire)
AZURE_TENANT_ID=

# Secret client (application Azure, flux confidentiel)
AZURE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Développement et tests uniquement
# -----------------------------------------------------------------------------

# Port du serveur (dev : 3001 ; serveur BDD : 3011). Electron utilise 3002.
# PORT=3001

# Utiliser le connecteur email mock au lieu du vrai (scripts, serveur BDD)
# BDD_MOCK_CONNECTEUR=1

# Stockage paramètres/compte en RAM (tests BDD, pas de fichiers)
# BDD_IN_MEMORY_STORE=1

# URL de base pour les tests E2E Playwright (défaut : http://127.0.0.1:3011)
# PLAYWRIGHT_BASE_URL=http://127.0.0.1:3011

# Exécuter un seul fichier .feature (ex : configuration-airtable)
# BDD_FEATURE=configuration-airtable

# Port du serveur lancé par Electron (défaut : 3002)
# ELECTRON_APP_PORT=3002

# Simuler le mode packagé : dossier de données (et .env.local) dédié
# JOB_JOY_USER_DATA=C:\chemin\vers\dossier-user-data

# Tests d’intégration Airtable (optionnel)
# AIRTABLE_API_KEY=
# AIRTABLE_BASE_TEST_URL=

# Inscription consentement (US-3.15) : l'app envoie l'email vers Val.town (URL en dur dans le code).
# Val.town stocke la clé Airtable et écrit dans la base. Aucune variable d'environnement requise côté app.
